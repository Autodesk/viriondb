"use strict";

var assert = require("assert");

describe("hello world tests", function () {
  it('should not really testing anything', function () {
    assert.equal("hello world", "hello world", "uhh, what?");
  });
});